#ifndef HEAD
#include <bits/stdc++.h>
using namespace std;

#define it(i, beg, end) for(int i = beg; i < end; ++i)
#define IT(i, beg, end) for(int i = beg; i > end; --i)
#define i64 long long
#define u32 unsigned
#define u64 unsigned long long
#define vecint vector<int>
#endif

const int MAX = 1e5 + 3;

void work()
{
	
}

main() {
	#ifndef ONLINE_JUDGE
	freopen("in.txt", "r", stdin);
	#endif

	int t; cin >> t; while (t--) 
	work();
}