#include <iostream>
#include <algorithm>
#include <numeric>
#include <vector>
#include <string>
using namespace std;

#define it(i, beg, end) for(int i = beg; i < end; ++i)
#define IT(i, beg, end) for(int i = beg; i > end; --i)

const int MAX = 1e5 + 3;

void work() {

}

main() {
	#ifndef ONLINE_JUDGE
	freopen("in.txt", "r", stdin);
	#endif
	
	int t; cin >> t; while (t--) 
	work();
}